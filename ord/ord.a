#!/bin/bash

# fix $ORD
__oRD_default=~/ord
[ ! -v ORD ] && export ORD=~/ord && echo \
"ORDnungizer's root is now '$ORD'
you can set it in you bash startup file, as
export ORD='$ORD'"

. "$ORD"/.bin/main.sh 
